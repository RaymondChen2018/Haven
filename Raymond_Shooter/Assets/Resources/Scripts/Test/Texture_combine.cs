using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Texture_combine : MonoBehaviour {
    public SpriteRenderer spr1;
    public SpriteRenderer spr2;
	// Use this for initialization
	void Start () {
        
        /*
        GetComponent<MeshRenderer>().material.texture
		Color[] pixels1 = (spr1.material).GetPixels();
        Color[] pixels2 = texture2.GetPixels();
        Sprite
        

        for (int i = 0; i < pixels1.Length; i++)
        {
             pixels2[i] += pixels1[i];
        }
        texture2.SetPixels(pixels2);
        */
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
